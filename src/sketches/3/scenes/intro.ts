import type p5 from "p5";
import type { ProtagonistEntity } from "../characters/protagonist";
import type { Stage } from "../stage";

export async function Intro(
	_p: p5,
	_protagonist: ProtagonistEntity,
): Promise<Stage> {
	throw new Error("sin implementar");
}
